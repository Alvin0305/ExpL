type
    bst {
        int value;
        bst left;
        bst right;
    }
endtype

decl
    bst root;
enddecl

int main() {
    decl
    enddecl

    begin
        root = alloc();
        write(root);
        free(root);
        root = alloc();
        write(root);
    end
}