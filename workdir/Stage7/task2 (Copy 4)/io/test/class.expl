class
    Person {
        decl
            str name;
            int age;

            int printPerson();
            str findName();
            int createPerson(str name, int age);
        enddecl

        int printPerson() {
            decl
            enddecl

            begin
                brkp;
                write(self.findName());
                write(self.age);
                return 1;
            end
        }

        str findName() {
            decl enddecl
            begin
                brkp;
                write(self.name);
                return self.name;
            end
        }

        int createPerson(str name, int age) {
            decl enddecl
            begin
                brkp;
                self.name = name;
                self.age = age;
                return 1;
            end
        }
    }
endclass

decl
    int x;
    str name;
    int array[10][2][3];
    int func(int a, int b);
    str fun(str a);
    Person me, him;
    //Dog toby, tom;
    int *ptr;
    str *sptr;
enddecl

int main() {
    decl
    enddecl

    begin
        x = initialize();
        me = alloc();
        
        me.name = "Kiran";
        me.age = 21;

        write(me.name);
        write(me.age);

        x = me.createPerson("Alvin", 20);
        
        name = me.findName();
        brkp;
        write(me.findName());
        write(me.name);
        write(me.age);
        write(name);

        write("hi");
    end
}