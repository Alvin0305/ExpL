begin
    decl
        int a, b, c;
    enddecl
    
    write("Enter 3 num:");

    read(a);
    read(b);
    read(c);

    if (a > b) then 
        write(a);
    else
        write(b);
    endif
    
    write("largest:");
    if (a > b and a > c) then
        write(a);
    endif

    if (b > a and b > c) then 
        write(b);
    endif

    if (c > a and c > b) then
        write(c);
    endif

end