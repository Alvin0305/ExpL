begin
    read(a);
    read(b);
    read(c);
    if (a < b) then 
        if (b < c) then s = c; else s = b; endif
    else 
        if (a < c) then s = c; else s = b; endif
    endif
    write(s);
end